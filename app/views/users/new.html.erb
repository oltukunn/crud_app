<h1>投稿画面</h1>
<%= form_with model: @user, url: users_path, method: :post do |f|  %>
  <p>お名前:<%= f.text_field :name %></p>
  <p>ご年齢:<%= f.text_field :age %></p>
  <input type="submit" values="送信する">
<% end %>
